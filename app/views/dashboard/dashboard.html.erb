<% if current_user %>
  <%= render partial: "text_posts/new_text_post", locals: { collapsible: true } %>
<% end %>

<% posts_json = render partial: "api/dashboard/dashboard.json", locals: { posts: @posts } %>
<%= react_component('Newsfeed', posts: JSON.parse(posts_json)["posts"]) %>

<%- render partial: "newsfeed", locals: { posts: @posts } %>
