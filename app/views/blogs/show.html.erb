<% content_for :title do %>
  Quill: <%= @user.username %>
<% end %>

<%= react_component('BlogHeading',
  user: JSON.parse(render template: "api/users/user.json", locals: { user: @user, posts: @posts })
) %>

<%= react_component('PostList', 
  initial_state: JSON.parse(render template: "api/blogs/show.json", locals: { posts: @posts }),
  big_title: false,
  collapsible_reblogs: true,
  display_author: false) %>
